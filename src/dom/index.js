import { attr } from "./attr";
import { hasClass, removeClass, addClass, toggleClass } from "./class";
import { html, text } from "./content";
import { css } from "./css";
import { on } from "./event";
import { parent, elementIndex, next, prev, siblings } from "./nodeObtain";
import { append, prepend, insertBefore, remove } from "./nodePend";
import { offset, position } from "./position";
import { width, height } from "./size";

export default {
  attr,
  hasClass,
  removeClass,
  addClass, 
  toggleClass,
  html,
  text,
  css,
  on,
  parent,
  elementIndex,
  next,
  prev,
  siblings,
  append,
  prepend,
  insertBefore,
  remove,
  offset,
  position,
  width,
  height
};